const Tag = ({ children }) => (
  <span className="inline-block px-3 py-1 bg-gray-200 text-gray-500 dark:bg-gray-900 dark:text-gray-600 rounded-full">
    {children}
  </span>
);

export default Tag;
